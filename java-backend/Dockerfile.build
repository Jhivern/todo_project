# Use Eclipse-Temurin (JDK 23)
FROM eclipse-temurin:23-jdk

# Install Maven, Git, curl
RUN apt-get update && \
    apt-get install -y maven git curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /workspace